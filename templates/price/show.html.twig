{% extends 'layout_admin.html.twig' %}

{% block title %}Tarifs{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center m-5">Tarifs</h1>
        <div class="admin-section pt-5 pb-5 px-5">
            <table class="table">
                <tbody>
                <tr>
                    <th>Adulte</th>
                    <td>{{ price.adult }}</td>
                </tr>
                <tr>
                    <th>Enfant</th>
                    <td>{{ price.child }}</td>
                </tr>
                <tr>
                    <th>Groupe</th>
                    <td>{{ price.crowd }}</td>
                </tr>
                </tbody>
            </table>
            <div class="d-flex justify-content-center">
                <a href="{{ path('admin_price_edit', {'id': price.id}) }}" class="btn btn-outline-danger">Modifier</a>
            </div>
        </div>
            <div class="d-flex justify-content-around">
                <a href="{{ path('admin_price_index') }}" class="btn btn-dark m-5">Liste des tarifs</a>

                {{ include('price/_delete_form.html.twig') }}
            </div>
        </div>
{% endblock %}
