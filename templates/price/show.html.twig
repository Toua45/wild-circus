{% extends 'layout_admin.html.twig' %}

{% block title %}Tarifs{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center m-5">Tarifs</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Adulte</th>
                <td>{{ price.adult }}</td>
            </tr>
            <tr>
                <th>Enfant</th>
                <td>{{ price.child }}</td>
            </tr>
            </tbody>
        </table>

                <a href="{{ path('admin_price_index') }}" class="btn btn-dark m-5">Liste des tarifs</a>

                <a href="{{ path('admin_price_edit', {'id': price.id}) }}" class="btn btn-outline-primary">Modifier</a>

                {{ include('price/_delete_form.html.twig') }}
    </div>
{% endblock %}
