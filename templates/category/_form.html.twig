{{ form_start(form) }}
    {{ form_widget(form) }}
<div class="flex d-flex justify-content-around">
    <button class="btn btn-outline-danger mt-5 mb-5">{{ button_label|default('Enregistrer') }}</button>
</div>
{{ form_end(form) }}
