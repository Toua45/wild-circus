{% extends 'layout_admin.html.twig' %}

{% block title %}Univers{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center m-5">L'univers {{ category.name }}</h1>
        <div class="admin-section pt-5 pb-5 px-5">
            <table class="table">
                <tbody>
                <tr>
                    <th>Nom</th>
                    <td>{{ category.name }}</td>
                </tr>
                </tbody>
            </table>
            <div class="d-flex justify-content-center">
                <a href="{{ path('admin_category_edit', {'id': category.id}) }}" class="btn btn-outline-danger">Modifier</a>
            </div>
        </div>
        <div class="d-flex justify-content-around">
            <a href="{{ path('admin_category_index') }}" class="btn btn-dark m-5">Liste des univers</a>

            {{ include('category/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
